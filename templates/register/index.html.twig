{% extends 'base.html.twig' %}
{% block content %}
    <h1>Inscription</h1>
    {% if notif %}
        <div class="alert alert-info">{{ notif }}</div>
        <hr>
    {% endif %}
    {{form(form)}}
{% endblock %}